import React, { PropTypes } from 'react'

/**
 * list search
 */
export default class Filter extends React.Component {
  static propTypes = {
  }

  render () {

    return (
      <div id="operations-labels-container" className="ui small labels">
      </div>
    )
  }
}
